import React from "react";
import styled from "styled-components";

function Footer() {
  return (
    <Container>
      <TextContainer>I am footer</TextContainer>
    </Container>
  );
}

export default Footer;

const Container = styled.div``;

const TextContainer = styled.h2``;
